# Database file — compressed version included; uncompressed is rebuilt at runtime
*.db
!olist.db.gz

# Source data CSVs (download from Kaggle — too large for GitHub)
data/

# Python virtual environment
.venv/

# Python cache
__pycache__/

# Query output text files (generated locally)
output/

# Python scripts — explicitly include only the portfolio deliverables
*.py
!app.py
!data_loader.py
!run_all_queries.py
